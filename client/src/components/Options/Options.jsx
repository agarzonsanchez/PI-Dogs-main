import React, { useState } from "react";
import { useSelector } from "react-redux";

const Options = () => {
  //const [temS, settemS] = useState(dogtemperaments);
  //console.log(props);
  return <></>;
};

export default Options;
